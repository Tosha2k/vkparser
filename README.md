## parser example
### Use symfony and:
+ Cache
+ Console
+ Doctrine ORM
+ Twig

### install and use
+ clone
+ composer install
+ bin/console doctrine:migrations:migrate
+ set vk access token in ENV
+ bin/console app:pars
